x=input('Введите x:');
if (x>=-2 && x<1)
    disp('входит в 1 промежуток')
elseif (x>=-1 && x<1)
    disp('входит во второй промежуток')
elseif (x>=1 && x<=2)
    disp('входит в 3 промежуток')
else     
    disp('не входит ни в один из промежутков')
    
end